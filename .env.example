# PostgreSQL connection string
DATABASE_URL=postgres://cinema:cinema@localhost:5432/cinema?sslmode=disable

# Server port
SERVER_PORT=8080

# How long a seat hold lasts before expiring (Go duration format)
HOLD_TTL=2m

# For integration tests (can point to a separate test database)
TEST_DATABASE_URL=postgres://cinema:cinema@localhost:5432/cinema?sslmode=disable
